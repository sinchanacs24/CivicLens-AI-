fastapi==0.111.0
uvicorn[standard]==0.30.1
pandas==2.2.2
numpy==1.26.4
pydantic==2.7.4
python-multipart==0.0.9
python-dotenv==1.0.1
google-cloud-storage==2.17.0
google-cloud-bigquery==3.25.0
google-genai==1.2.0
httpx==0.27.0

# Optional GPU acceleration (install separately on a CUDA-enabled machine):
#   pip install cudf-cu12 --extra-index-url=https://pypi.nvidia.com
# The backend automatically falls back to Pandas if cuDF / a GPU is unavailable.
